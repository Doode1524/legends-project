<p> <%= @user.username %>'s teams:
    <% @teams.collect do |team| 
        if team.user_id  == @user.id %>
        <div>
            <%= team.team_name %>
        </div>

        <% end %> 

    <% end %>
</p>

